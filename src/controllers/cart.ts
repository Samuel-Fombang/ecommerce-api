export const addToCart = async (req: any, res: any) => {
  res.json({ message: "addToCart working" });
};

export const getCart = async (req: any, res: any) => {
  res.json({ message: "getCart working" });
};

export const removeFromCart = async (req: any, res: any) => {
  res.json({ message: "removeFromCart working" });
};

export const clearCart = async (req: any, res: any) => {
  res.json({ message: "clearCart working" });
};